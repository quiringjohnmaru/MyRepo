to change the git user:

   git config --global user.name user_name

to change the git email:

   git config --global user.email email@example.com

to create a remote name:

   git remote add remote_name https://github/user_name/repo_name.git

to push commit remotely:

   git push remote_name master

to pull files remotely:

   git pull remote_name master

Footer:
